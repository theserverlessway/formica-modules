resource(route53.RecordSetType(
    name(source, "MXRecord"),
    Name= source,
    Type= 'MX',
    HostedZoneId= ref(hostedZone),
    ResourceRecords= target if isinstance(target, list) else [target],
    TTL= 3600))
